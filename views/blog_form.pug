extends layout

block content
  h2= title
  
  p Image Upload coming soon..
  
  form(method='POST' action='')
    input(type='text' placeholder='title' name='title')
    textarea(name='message')
    input(type='submit' value='Send')
    
  if errorMessage
    p= errorMessage
